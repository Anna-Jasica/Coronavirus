import React from "react";
import "./index.css";
import ListWrapper from "../src/components/ListWrapper/ListWrapper";

const App = ({ content }) => (
    <div>
        <ListWrapper />
    </div>
);

export default App;
